import 'package:flutter/material.dart';

class MixListtem extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Container(
      child: Placeholder(
        color: Colors.red,
      ),
    )
  }

}